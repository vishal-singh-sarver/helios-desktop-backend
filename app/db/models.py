"""
SQLAlchemy ORM models.

Tables:
  projects          — one row per HeliosGUI project
  project_versions  — compressed XML snapshots (version history)
  project_objects   — object registry entries per project
"""
# TODO: implement in Step 2
